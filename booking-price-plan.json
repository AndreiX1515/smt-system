{
  "planName": "예약 금액 DB 저장 및 표시 개선",
  "targetServer": "테스트서버 (47.128.242.86)",
  "dbInfo": {
    "host": "localhost",
    "user": "root",
    "password": "cloud1234",
    "database": "smarttravel"
  },
  "phases": [
    {
      "phase": 1,
      "name": "DB 스키마 변경",
      "sql": "ALTER TABLE bookings ADD COLUMN IF NOT EXISTS adultPrice DECIMAL(10,2) NULL COMMENT '예약 시 적용된 성인 단가', ADD COLUMN IF NOT EXISTS childPrice DECIMAL(10,2) NULL COMMENT '예약 시 적용된 아동 단가', ADD COLUMN IF NOT EXISTS infantPrice DECIMAL(10,2) NULL COMMENT '예약 시 적용된 유아 단가', ADD COLUMN IF NOT EXISTS visaFee DECIMAL(10,2) DEFAULT 0 COMMENT '비자 비용 합계', ADD COLUMN IF NOT EXISTS flightOptionFee DECIMAL(10,2) DEFAULT 0 COMMENT '항공옵션 비용 합계';"
    },
    {
      "phase": 2,
      "name": "데이터 마이그레이션 - 단가",
      "sql": "UPDATE bookings b LEFT JOIN package_available_dates pa ON b.packageId = pa.package_id AND DATE(b.departureDate) = pa.available_date LEFT JOIN packages p ON b.packageId = p.packageId SET b.adultPrice = COALESCE(pa.b2b_price, p.b2b_price, p.packagePrice), b.childPrice = COALESCE(pa.b2b_child_price, p.b2b_child_price, ROUND(COALESCE(pa.b2b_price, p.b2b_price, p.packagePrice) * 0.7, 0)), b.infantPrice = COALESCE(pa.b2b_infant_price, p.b2b_infant_price, 10000) WHERE b.bookingStatus NOT IN ('cancelled', 'rejected');"
    },
    {
      "phase": 3,
      "name": "데이터 마이그레이션 - 비자비용",
      "sql": "UPDATE bookings b SET b.visaFee = (SELECT COALESCE(SUM(CASE WHEN LOWER(bt.visaType) = 'group' THEN 1500 WHEN LOWER(bt.visaType) = 'individual' THEN 1900 ELSE 0 END), 0) FROM booking_travelers bt WHERE bt.transactNo = b.bookingId) WHERE b.bookingStatus NOT IN ('cancelled', 'rejected');"
    },
    {
      "phase": 4,
      "name": "데이터 마이그레이션 - 항공옵션비용",
      "sql": "UPDATE bookings b SET b.flightOptionFee = (SELECT COALESCE(SUM(bto.price), 0) FROM booking_traveler_options bto WHERE bto.booking_id = b.bookingId) WHERE b.bookingStatus NOT IN ('cancelled', 'rejected');"
    }
  ],
  "fileModifications": [
    {
      "order": 1,
      "file": "/var/www/html/admin/backend/api/super-api.php",
      "action": "getB2BBookingDetail() 함수의 SELECT에 adultPrice, childPrice, infantPrice, visaFee, flightOptionFee 컬럼 추가"
    },
    {
      "order": 2,
      "file": "/var/www/html/admin/backend/api/agent-api.php",
      "action": "예약 조회 시 새 컬럼 SELECT 추가, 예약 생성 시 새 컬럼 INSERT 추가"
    },
    {
      "order": 3,
      "file": "/var/www/html/admin/super/b2b-booking-detail.html",
      "action": "renderAmountBreakdown() 함수에서 자체 계산 로직 제거하고 DB 값(booking.adultPrice, booking.childPrice 등) 그대로 사용"
    },
    {
      "order": 4,
      "file": "/var/www/html/admin/js/agent-create-reservation.js",
      "action": "예약 저장 시 adultPrice, childPrice, infantPrice, visaFee, flightOptionFee 값 API로 전송"
    },
    {
      "order": 5,
      "file": "/var/www/html/admin/agent/reservation-detail.html",
      "action": "DB 값 표시 확인 (필요시 수정)"
    }
  ],
  "verification": {
    "dbCheck": "mysql -u root -pcloud1234 smarttravel -e \"DESCRIBE bookings\" | grep -E 'adultPrice|childPrice|infantPrice|visaFee|flightOptionFee'",
    "migrationCheck": "mysql -u root -pcloud1234 smarttravel -e \"SELECT bookingId, adultPrice, childPrice, infantPrice, visaFee, flightOptionFee FROM bookings WHERE adultPrice IS NOT NULL LIMIT 5;\"",
    "testUrls": [
      "http://47.128.242.86/admin/agent/create-reservation.html",
      "http://47.128.242.86/admin/super/b2b-booking-detail.html?id=BK...",
      "http://47.128.242.86/admin/agent/reservation-detail.html?id=BK..."
    ]
  }
}
